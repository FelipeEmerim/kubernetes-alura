apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-noticias.fullname" . }}-test-connection"
  labels:
    {{- include "db-noticias.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-noticias.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
