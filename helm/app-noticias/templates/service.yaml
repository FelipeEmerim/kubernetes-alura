apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-noticias.fullname" . }}
  labels:
    {{- include "app-noticias.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-noticias.selectorLabels" . | nindent 4 }}
